# 数据与模型权重不进 git:脚本可重建,权重放 HF Hub / ModelScope
# 根目录锚定:不带斜杠前缀会连 src/medforge/data/ 源码一起屏蔽(首次 commit 就踩过)
# 例外:校准集是手工标注资产,脚本重建不出来,必须进版本库
/data/*
!/data/calibration/
/models/
*.safetensors
*.gguf

# 实验日志(SwanLab 云端为准)
swanlog/
output/
wandb/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
dist/

# 密钥
.env

# 系统
.DS_Store

# 评测原始作答体积大(73MB),不进 git:scored/summary 才是可追溯产物
reports/runs/*/*.outputs.jsonl

# 前台构建产物与依赖
web/node_modules/
web/dist/
# 回放数据破例入库(见下 !):Vercel 构建时不跑 Python,拉不到数据页面就是空的;
# 3.2MB 一次性成本换「静态站零后端」的部署形态,值
