# 弃权数据集报告(sft_abstain_v1)

生成:2026-09-05T10:33:20+08:00 · git d09f712

自采样:蒸馏模型(`output/sft_distill_v1/merged`)在去污染题池上每题 K 次作答;判定用严格可用协议 v3 的规则层(收尾 ∧ 声明 ∧ 判对),全程不调 LLM judge;user 提示词与评测共用 `eval.run.PROMPT_CHOICE/PROMPT_OPEN`,训练与评测同一份。

参数:source=(全部) · known 判对占比≥100% · unknown 判对≤0 · 未收尾≤1 · 声明≥1 · min-samples=4 · abstain-ratio=0.35 · general-ratio=0.15 · 总长≤8192 · seed=42

## 1. 样本漏斗

采样条数 16000 条(覆盖 4000 道题)

| 判定 | 剔除 | 剩余 |
|---|---|---|
| 前置:id 不在题池里(样本文件比题池旧 / 换了 --source) | 0 | 16000 |
| ① 收尾:finish_reason=length 或作答里没有 </think> | 248 | 15752 |
| ② 标签成对:答卷里有多个 </think>(收尾写了两遍),整条丢 | 1 | 15751 |
| ③ 声明:作答段抽不出答案(规则层弃权) | 25 | 15726 |

题池内 16000 条:收尾率 98.4% · 声明率 98.3%。这两条是**样本级**,分类是**题目级**,不要互相换算。

另有 0 道题的 id 不在题池里,未参与分类;参与分类的是 4000 道题(下表分母)。

每题采样条数分布(残缺题拿不到 K 次全对/全错的证据强度,由 `--min-samples` 拦):

| 条数 | 题数 |
|---|---|
| 4 | 4000 |

## 2. 题目分类直方图

| 类别 | 题数 | 占已分类题 | 含义 |
|---|---|---|---|
| known | 2643 | 66.1% | K 次全对:保留正解,当普通 SFT 样本 |
| unknown | 421 | 10.5% | 0 次对且基本写完了:改教「答案:不确定」 |
| unstable | 936 | 23.4% | 其余,全部丢弃(理由见下) |

| unstable 理由 | 题数 |
|---|---|
| 采样条数 < --min-samples | 0 |
| 半对半错(0 < 判对数 < 门槛):本来就有一半机会答对,教拒答是净损失 | 913 |
| 未收尾条数 > --unknown-max-unfinished:是「没写完」不是「不会」 | 23 |
| 没有一条既收尾又声明:全是截断 / 格式垃圾,不能当作不会 | 0 |
| 有样本规则层判不了(开放题语义等价要 LLM 层):「判不了」不等于「判错」 | 0 |

| 选样剔除(分完类之后) | 题数 |
|---|---|
| 格式:作答段缺「答案:」字面(只对 known 行要求) | 0 |
| 长度:提示词 + 思考 + 作答的估算总长超 --train-max-length | 4 |
| 选样:该题没有候选样本活到最后 | 0 |

分类之后又在选样阶段掉了 4 道题。

## 3. 配比与成品

选样后 known 2643 / unknown 417;按 `--abstain-ratio 0.35` 下采样多的一边,丢 1869 行。

| 类别 | 教材行数 | 占医疗行 |
|---|---|---|
| known(正常作答) | 774 | 65.0% |
| unknown(答案:不确定) | 417 | 35.0% |

**医疗 1191 行 + 通用回放 210 行 = 1401 行** → `data/processed/sft_abstain_v1.jsonl`

## 4. 长度分布(token;有 completion_tokens 时以端点报的为准)

| 类别 | p50 | p90 | p99 | max |
|---|---|---|---|---|
| known 单条总长 | 467 | 2028 | 5084 | 7515 |
| unknown 单条总长 | 2380 | 6312 | 8038 | 8191 |

总长 max 8191 ≤ 训练 max_length 8192:超限的在数据侧已剔除,不交给框架 truncation(它截掉的正是末段那句「答案:…」)。

## 5. 待办

- 过渡句(`BRIDGE_SENTENCE`)只有一句、固定不变,模型可能把它当触发词背下来,在会做的题上一写出这句就滑向弃权。验收要看 `abstain_report` 的**选择性准确率**是否上升、覆盖率的跌幅是否小于弃权精度带来的收益;若否,先试多样化过渡句,再调 `--abstain-ratio`。
- `--abstain-ratio 0.35` 是起手值,没有实证依据。跑完第一版后用 `python -m medforge.eval.abstain_report --run abstain-v3-sample --ref distill-v3-sample` 看覆盖率 / 选择性准确率,再决定往哪边调。
- unknown 的思考段是模型答错时的推理,里面可能含事实错误。本轮不做人工抽检——若发现模型学会「编一段错推理再说不确定」,应改为截短思考或换成模板化思考。
